#create a vector
apple_colors <- c('green', 'green', 'yellow', 'red', 'red', 'red', 'green')

#create a factor for tje object

factor_apple <- factor(apple_colors)

#Print the factors out
print(factor_apple)
print(nlevels(factor_apple))
